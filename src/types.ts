export interface MockSystem {
  id: string;
  name: string;
  icon: string;
  inputDescription: string;
  inputText: string;
  outputText: string;
}

export interface ContactInfo {
  phone: string;
  phoneDisplay: string;
  email: string;
  github: string;
  web: string;
}
